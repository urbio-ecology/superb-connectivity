% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patch-connectivity.R
\name{patch_connectivity}
\alias{patch_connectivity}
\title{A set of connected habitat patches}
\usage{
patch_connectivity(data, species, interpatch_distance, res = NA)
}
\arguments{
\item{data}{Data frame of connected patches. Must contain an \code{area} column.}

\item{species}{Character of length 1. Species the analysis was run for.}

\item{interpatch_distance}{Numeric of length 1. The interpatch distance (m)
the analysis used.}

\item{res}{resolution in pixels - defaults to NA, not required for vector
based approaches.}
}
\value{
A \code{patch_connectivity} object: a tibble with \code{species} and
\code{interpatch_distance} attributes.
}
\description{
The object returned by \code{\link[=habitat_connectivity]{habitat_connectivity()}}: a \link[tibble:tibble]{tibble}
of connected patch areas that also carries the \code{species} and
\code{interpatch_distance} the analysis was run with as attributes.
}
\details{
Because it is a tibble subclass it behaves like a data frame directly --
\code{$}, \code{[}, \code{\link[DT:datatable]{DT::datatable()}}, \code{\link[utils:write.csv]{utils::write.csv()}} and ggplot2 all work
without ceremony. Read the metadata back with \code{\link[=pc_species]{pc_species()}} and
\code{\link[=pc_interpatch_distance]{pc_interpatch_distance()}}.
}
