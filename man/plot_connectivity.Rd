% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-summaries.R
\name{plot_connectivity}
\alias{plot_connectivity}
\title{Plot connectivity metrics across buffer distances}
\usage{
plot_connectivity(results_connect_habitat)
}
\arguments{
\item{results_connect_habitat}{Data frame. Connectivity summary results with
columns for species_name, buffer_distance, and various metrics.}
}
\value{
A ggplot2 object with faceted plots of connectivity metrics.
}
\description{
Creates faceted line plots showing how connectivity metrics change with
different buffer distances. This works best when you have multiple buffer
distances, otherwise it will just be a plot with one point.
}
\examples{
lizard_habitat <- example_habitat()
lizard_barrier <- example_barrier()
results <- purrr::map(
  c(10, 20),
  function(d) {
    full <- habitat_connectivity_full(lizard_habitat, lizard_barrier,
      distance = d, verbose = FALSE)
    summarise_connectivity(
      area = full$areas_connected$area,
      buffer_distance = d,
      target_resolution = 500,
      data_resolution = 10,
      aggregation_factor = 50,
      species_name = "Blue-tongued Lizard"
    )
  }
) |> purrr::list_rbind()
plot_connectivity(results)
}
