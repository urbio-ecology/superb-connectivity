% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sf-connectivity.R
\name{sf_habitat_buffer}
\alias{sf_habitat_buffer}
\title{Buffer habitat by distance}
\usage{
sf_habitat_buffer(habitat, buffer_radius)
}
\arguments{
\item{habitat}{SF object. Habitat spatial data.}

\item{buffer_radius}{Numeric. The radius in metres around the habitat.
Specify it as half the interpatch distance (see \code{\link[=habitat_buffer]{habitat_buffer()}}).
Because vector buffering is done in continuous space, there is no minimum
representable radius — unlike the raster path, there is no resolution below
which the buffer becomes a no-op.}
}
\value{
SF object with buffered and unioned habitat geometry.
}
\description{
Creates a buffer around habitat polygons and unions overlapping areas into
a single polygon, using \code{\link[sf:st_buffer]{sf::st_buffer()}}. Unlike the raster
\code{\link[=habitat_buffer]{habitat_buffer()}}, this works in continuous coordinate space, so there is
\strong{no resolution constraint}: any \code{buffer_radius} produces an exact buffer
and the sub-cell "no buffer" problem of the raster path does not apply. The
buffer arc is approximated by \code{nQuadSegs} straight segments per quarter
circle (here 5, i.e. a 20-sided polygon), which affects the \emph{smoothness} of
the outline, not whether the buffer forms. See
\code{vignette("interpatch-distance-and-resolution")}.
}
\examples{
lizard_habitat_sf <- terra::as.polygons(example_habitat(), dissolve = TRUE) |>
  sf::st_as_sf()
\dontrun{
sf_habitat_buffer(lizard_habitat_sf, buffer_radius = 10)
}
}
